@model AppVendasWeb.Models.Categoria

@{
    ViewData["Title"] = "Categoria";
}

<h4 class="text-center">Categoria</h4>


<div class="row">
    <div class="col12">
        <form asp-action="Create" asp-action="Index">
            <div value="Create" class="row g-2">


                <div class="col-md">
                    <div class="form-group">
                        <label asp-for="CategoriaNome" class="control-label"></label>
                        <input asp-for="CategoriaNome" class=" form-control p-2 py-3 rounded-end" />
                        <span asp-validation-for="CategoriaNome" class="text-danger"></span>
                    </div>
                </div>
                </div>
            <div class="row g-2">
                <div class="col">
                </div>
                <div class="row g-2">
                </div>
                <div class="col">
                    <div class="form-group" style="display:flex;    justify-content: flex-start; margin-top:30px">
                        <button type="submit" value="Create" class="btn btn-success w-50 h-100">
                            <svg bootstrap-icon="CheckLg" width="20" height="20" aria-label="Salvar"></svg>
                            <br /> Salvar
                        </button>
                    </div> 
                    <div class="form-group"  style="display:flex;    justify-content: flex-start; margin-top:30px">
                        <button  value="Index" class="btn btnc w-50 h-100">
                        
                            <br /> Voltar
                        </button>
                    </div>
                </div>
            </div>
  </form>
    
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
